#!/usr/bin/env python3
"""
    Type annotated function that takes two strings (str1, str2),
    and returns a concatenated string of both
"""


def concat(str1: str, str2: str) -> str:
    """ Concatenates two strings """
    return str1 + str2
