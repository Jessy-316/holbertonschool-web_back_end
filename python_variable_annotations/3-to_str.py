#!/usr/bin/env python3
"""
    Type annotated function (to_str) which takes a float (n),
    and returns a string representation of that float
"""


def to_str(n: float) -> str:
    """ Returns a string representation of the float """
    return str(n)
